--- a/st-hal/sound_trigger_hw.h
+++ b/st-hal/sound_trigger_hw.h
@@ -311,11 +311,7 @@
 } while (0)
 
-#ifdef LINUX_ENABLED
 extern int dbg_trace_max_lab_reads;
-#else
-int dbg_trace_max_lab_reads;
-#endif
 
 #define ST_DBG_ATRACE_ASYNC_BEGIN_IF(cond, name, cookie)\
